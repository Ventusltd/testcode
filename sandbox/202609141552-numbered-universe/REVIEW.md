# Numbered-code explorer: reproducible viewer packaging

This timestamped viewer reuses the verified immutable data in ../202609141527-numbered-universe/data/. It preserves all 249018 line keys, 15601 family keys, 5506 tablets and 41984 pinned source occurrences from Stars 5ed441256c44a27bb8140722919d8e07edb8f8f5. No large data copy is added.

The previous viewer worked in public desktop and touch tests, but two publication hashes described Windows working-tree line endings rather than the committed LF bytes. This version normalizes source text and computes its manifest from staged Git blobs. Published historical files are unchanged.

The data manifest and all 257 data shards matched served bytes at 15:49:00 UTC. The public selected journey completed at both 1440px and 430px at 15:50:27 UTC, with actual GitHub source opening and Back. The relationship select is exercised through its change event; graph/list navigation uses mouse or emulated touch. These checks do not claim every possible code path.
