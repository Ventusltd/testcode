# Hop-count topology lab

Data-only transcription of PRODUCT in `proofs/electrical-distance.proof.mjs` at `Ventusltd/ventus-grid-engine` full pin `d9cd18b0e2034325814924e6e4a0e958014f2748`. Site identifiers are synthetic proof labels, not a real network map. Six current nodes, five sites, four circuits, one transformer and one excluded planned edge. The proposed endpoint is not a current node. Diagram positions and drawing animation are authored here, not geographic coordinates or a physical simulation.

Direct immutable URL imports of `engine/network-topology.js` and `engine/electrical-distance.js` own the graph and searches. No implementation copy or fallback search. `between` yields one fewest-hop path within the chosen budget; `within` yields site hop counts, not full routes. Circuit and transformer edges both count as a hop. Source origin-voltage option filters starting nodes only. Different declared voltages across a circuit cause a traversal refusal; a transformer may cross. Unknown voltages are not inferred or proven equal.

The interface adds a strict integer0–6hop policy because the source does not comprehensively validate arbitrary option types/ranges. Unknown sites/schema may produce null; malformed source interfaces can throw. This finite fixed fixture is not an arbitrary network-data upload or validator. Returned seasonal MVA and impedance percentages on a100MVA base are retained unsummed. The9999MVA proof placeholder remains unchanged; a route does not establish spare capacity, network consistency or a connection right. Same-site routes have0hops and no path rows.

Initial COWL→STRA returns2hops viaDIDC. COWL132kV→DIDC crosses the transformer then a circuit,2hops and1declared voltage change. COWL→ISLE is not reached because the direct400→132kV circuit is refused. A one-hop budget excludes STRA. These observations describe only this fixture. Optional agent controls use the same query function and page state. Native support, device coverage and hosted checks must be evidenced separately.
