# Evidence record

Source pin `d9cd18b0e2034325814924e6e4a0e958014f2748`. The local visible Chrome preview loaded both pinned imports and showed2hops COWL4→DIDC4→STRA4, with the mixed-voltage circuit toISLE1 refused. That is a first-result observation, not comprehensive browser validation.

Tests hash-check both external modules before executing source-pinned queries. The browser harness exercises phone/desktop viewport layouts in Chromium and WebKit, ordered route, transformer, budget clipping, invalid budget, absent origin voltage, refused edge, same-site0hops,9999MVA preservation, exported JSON, reduced motion, no overflow, source-response hashes and blocked-module refusal. A committed test is not a passing execution receipt; hosted results follow after completion.

Native optional WebMCP has not been tested in a supported context. Synthetic fixture results do not validate real grid data, capacity or engineering compliance. Viewport emulation does not establish physical iOS memory-pressure behaviour.

Local source-hash-checked tests:13 passed, zero failures/skips. Twelve query/interface checks plus a deep equality check against the independently captured proof PRODUCT. Hosted tests run the12 portable checks; the extra local equality check uses a separately retained fixture. No breadth-first-search implementation is copied into this lab.
