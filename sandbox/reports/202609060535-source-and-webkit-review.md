# Source closure and rendered-frame evidence

Application source:946a14e566ffe5b66372bb3e99e712b685d56dca,
generation202609060528. The actual File / source-download action passed9 checks:
64 complete resource bodies match their own hashes, all4 executable cartridges
match the pinned composition, the router document and all8 tool-host modules
match committed bytes, and the saved map snapshot retains the exact polygon.
The43,772,310-byte download has SHA256
7c3bf10297c98549ed5f6c744fca90552bb7d98b8a8582b892387e5756be20fe.
It correctly declares incomplete browser discovery: one local diagnostic404
and11 discovery warnings remain. No server or dormant-dependency completeness
claim follows from these checks.

An initial additional host assertion failed because the local candidate server
served the2015 host modules with checkout CRLF endings. All8 printed bodies
matched the actual local files and normalized exactly to their Git blobs.
The server now pins its startup commit, caches committed Git blobs and uses
working files only for uncommitted candidate previews. It therefore serves the
same byte representation used by publication. The generalized source driver
derives the immutable generation from its URL instead of assuming0418.
Raw initial and corrected downloads are retained under poly0528-source-closure
and poly0528-source-canonical in offline-screenshots/recovery-20260906.

Visual inspection qualified the earlier WebKit interaction evidence. Both0418
and0528 produce a blank map in this Windows Playwright WebKit26.5 page screenshot,
even after waiting for loaded sources and a fresh render. Their actual canvas
captured inside the map render event contains the polygon, boundary and handles.
WebGL readback reports no error or lost context; the center pixel is[82,61,49,255].
A separate minimal red WebGL canvas appears correctly in the page screenshot
and native readback in WebKit, Chromium and Firefox. This rules out a blanket
absence of WebGL and shows the map screenshot is not reliable visual evidence
in this runner. It does not establish how every Safari build composites the app.

The82 WebKit state/control checks remain valid within their stated scope.
Do not describe the blank page screenshots as proof of map visibility, or replace
their blank region with the native canvas and claim it was an actual screenshot.
Chromium live acceptance and Firefox screenshots are separate evidence. No
production rendering settings were changed on the basis of this probe.
Raw scripts/results/native canvases/screenshots:webkit-pixel-probe.cjs,
webkit-pixel-probe/, webkit-map-probe.cjs and webkit-map-probe/ under the same
offline evidence root. Renderer strings are browser-reported diagnostics,
not independent identification of physical hardware.
