# Synthetic price-spread lab

Direct import of the reviewed `interconnector-economics.js` at `d9cd18b0e2034325814924e6e4a0e958014f2748`. No engine implementation copied. A generic synthetic 1 GW, one-hour, full-utilisation case at stated90/60GBP per MWh reproduces the proof's1GWh and30,000GBP gross arithmetic. No actual interconnector inventory, current market data or licensed route geometry is used. The page charts numerical sensitivity only; it never draws a cable or route.

`flowDirection` returns a commercial-incentive label and signed/absolute spread; it does not schedule flow. `energyTransferredGwh` calculates energy from stated capacity, period and utilisation. `congestionRentGbp` returns gross spread value before costs, not profit, revenue prediction or project valuation. Ten additional direct module calls sample utilisation0.1–1; the page only scales their returned values into chart coordinates. A selected utilisation below0.1 remains numerically valid but has no marker on that sampled domain.

Equal prices are a deliberately visible partial result: direction reports no commercial incentive, energy remains a stated-utilisation scenario, and the rent function refuses zero spread. The exact refusal is retained; no zero-value substitute is invented. Zero capacity/hours/utilisation, missing and nonfinite inputs are refused. The interface adds explicit maximums100GW/8760hours/absolute10000GBP per MWh and finite-output guards. These limits are not engine or engineering guarantees.

No price forecasting, profit, losses/availability, scheduled dispatch or geometry. These are the five module-declared excluded topics; source licensing prose is not republished as a separately established legal claim. RUN uses the visible validated action; CODE opens the exact pinned module. Other view handoffs require an established identity and target and are not fabricated. Native optional agent tools, when present, share the same action. Verification evidence is recorded after execution, not inferred from source publication.
